[workspace]

members = [
  "core",
  "query-ast",
  "query-cypher",
  "index-rocksdb",
  "index-garnet",
  "middleware",
  "shared-tests",
  "query-perf"]

[workspace.package]
version = "0.2.0"

[profile.release]
codegen-units = 1
lto = "thin"
