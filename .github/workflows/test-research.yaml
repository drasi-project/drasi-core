name: Test Issue Research

on:
  workflow_dispatch:
    inputs:
      issue_url:
        description: "Issue URL to research"
        required: true
        type: string

jobs:
  research:
    uses: drasi-project/.github/.github/workflows/drasi-issue-researcher.lock.yml@centralized-workflows
    with:
      issue_url: ${{ inputs.issue_url }}
    secrets: inherit